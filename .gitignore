node_modules/
dist/
build/
.next/
.turbo/
*.log
*.tsbuildinfo

# graphify: grafo generado, se reconstruye solo (config sí va en git)
graphify-out/

.DS_Store
Thumbs.db

# env
.env
.env.local
.env.*.local
!.env.example
# backups y pulls de secretos — NUNCA al repo (claves de prod)
*.pull
.env*.bak*
.env*.backup
# chequeo puntual de env de prod — contiene secretos, nunca al repo
.env.prod-check

# prisma
packages/db/generated/
*.db
*.db-journal
*.dump*
atelier-db-*.json
output/postgresql-tools/
output/postgresql18-tools/
output/postgresql-*-binaries.zip
output/atelier-backups/
# Artefactos de revisión, diagnósticos y datos operativos: solo locales.
/output/
/tmp/
/apps/api/land-*.png

# Material de firma y credenciales móviles: nunca versionar.
**/credentials.json
*.p8
*.p12
*.jks
*.keystore
*.key
*.mobileprovision

# editor
.vscode/
.idea/
*.swp

# expo
apps/mobile/.expo/
apps/mobile/web-build/
apps/mobile/ios/
apps/mobile/android/
apps/mobile/.expo-shared/

# vercel
.vercel

# tests
coverage/
/tmp/pdfs/
/tmp/android-menu-style-*/
.env*.local
# rotaciones de secretos — backups locales temporales no deben ir al repo
.env*.local.bak.*

# superpowers brainstorm
.superpowers/

# codex CLI — config/artefactos locales del agente, no van al repo
.codex/
